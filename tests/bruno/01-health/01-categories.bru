meta {
  name: Get Categories
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/categories
  body: none
  auth: none
}

tests {
  test("categories returns 200", function() { expect(res.status).to.equal(200); });
  test("categories response has categories", function() { expect(res.body.categories).to.be.an("array"); });
}
