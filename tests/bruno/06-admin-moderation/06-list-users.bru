meta {
  name: List Users
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/admin/users
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("users are listed", function() { expect(res.status).to.equal(200); expect(res.body.users).to.be.an("array"); });
}
