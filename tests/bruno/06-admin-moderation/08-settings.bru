meta {
  name: Read Settings
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/admin/settings
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("settings are returned", function() { expect(res.status).to.equal(200); expect(res.body.settings).to.be.an("array"); });
}
