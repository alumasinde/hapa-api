meta {
  name: Admin Logout
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/admin/auth/logout
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("admin logout succeeds", function() { expect(res.status).to.equal(204); });
}
